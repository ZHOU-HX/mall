spring:
  datasource:
    username: root
    password: zhx
    url: jdbc:mysql://192.168.109.139:3306/mall_oms
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml    # 如果写成“classpath:/mapper/**/*.xml”代表不止扫描自己的classpath，还扫描别人的
  global-config:
    db-config:
      id-type: auto     # 代表主键自增

# 端口号
server:
  port: 9000